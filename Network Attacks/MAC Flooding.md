- an attack that compromises the security of a switch by overflowing the switch's MAC table
- when mac table fills the switch acts as an hub and starts sending to all devices connected to the network

MAC FLooding resoaons 
-  DAta snooping: when it acts as hub an attacker can start to listen traffic because a network card in promisxuous mode 
- Disruption of services: cause a DoS with unnesscary traffic 
- Bypass security measures: it can make it ignore allow/deny list 

